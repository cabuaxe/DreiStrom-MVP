import { Routes } from '@angular/router';
import { InvoicingComponent } from './components/invoicing.component';

export const INVOICING_ROUTES: Routes = [
  { path: '', component: InvoicingComponent },
];
